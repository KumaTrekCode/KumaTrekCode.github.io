## 報告フォーマットのルール（モバイル対応）

- 最終報告はテキストとしてそのまま選択・コピーしやすい形式で出す
- GitHub Flavored Markdownの表（|区切り）は使わない
  → 箇条書き or 「項目: 状態」の1行形式を使う
- 報告の要点はコードブロックで囲み、長押しで全文選択しやすくする
- 絵文字は状態表示（✅❌⚠️）のみに留め、装飾目的では使わない
- チャット本文に必ずコピー可能な要約を含める
